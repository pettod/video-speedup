# Video speedup

Speedup parts in video with different factors.

## Run

In `config.py`define the timestamps and speedup factors. Then run the code.

```bash
python speedup_video_segments.py --run
```
